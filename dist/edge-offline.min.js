(function (root, factory) { if (typeof define === 'function' && define.amd) { define([], factory); } else if (typeof module === 'object' && module.exports) { module.exports = factory(); } else { root.EdgeOffline = factory(); } }(typeof self !== 'undefined' ? self : this, function () { 'use strict'; const DB_NAME = 'EdgeOffline_DB'; const DB_VERSION = 2; const STORE_NAME = 'sync_queue'; const listeners = new Map(); const handlers = new Map(); let dbPromise = null; let isSyncing = false; function emit(event, payload) { if (listeners.has(event)) { listeners.get(event).forEach(fn => { try { fn(payload); } catch (e) { console.error('[EdgeOffline:EventError]', e); } }); } } function getDB() { if (dbPromise) return dbPromise; dbPromise = new Promise((resolve) => { if (typeof indexedDB === 'undefined') { resolve(null); return; } const req = indexedDB.open(DB_NAME, DB_VERSION); req.onupgradeneeded = (e) => { const db = e.target.result; if (!db.objectStoreNames.contains(STORE_NAME)) { const store = db.createObjectStore(STORE_NAME, { keyPath: 'id', autoIncrement: true }); store.createIndex('action', 'action', { unique: false }); store.createIndex('priority', 'priority', { unique: false }); store.createIndex('timestamp', 'timestamp', { unique: false }); store.createIndex('status', 'status', { unique: false }); } }; req.onsuccess = () => resolve(req.result); req.onerror = () => { console.warn('[EdgeOffline] IndexedDB unavailable, using LocalStorage fallback'); resolve(null); }; }); return dbPromise; } function getNetworkInfo() { const nav = typeof navigator !== 'undefined' ? navigator : {}; const conn = nav.connection || nav.mozConnection || nav.webkitConnection; return { online: nav.onLine !== false, effectiveType: conn ? conn.effectiveType : '4g', rtt: conn ? conn.rtt : 50, downlink: conn ? conn.downlink : 10, saveData: conn ? conn.saveData : false }; } let statusBadge = null; function renderStatusUI() { if (typeof document === 'undefined') return; if (!statusBadge) { statusBadge = document.createElement('div'); statusBadge.id = 'edge-offline-badge'; statusBadge.className = 'edge-offline-pill'; statusBadge.style.cssText = ` position: fixed; bottom: 20px; right: 20px; z-index: 999999; display: inline-flex; align-items: center; gap: 8px; padding: 9px 16px; border-radius: 9999px; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; font-size: 12.5px; font-weight: 600; backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px); box-shadow: 0 10px 25px rgba(0,0,0,0.4), 0 0 15px rgba(99,102,241,0.25); transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); transform: translateY(140%); opacity: 0; pointer-events: none; `; document.body.appendChild(statusBadge); } return statusBadge; } function updateBadge(state, text, count = 0) { const el = renderStatusUI(); if (!el) return; if (state === 'offline') { el.style.background = 'rgba(239, 68, 68, 0.92)'; el.style.color = '#ffffff'; el.style.border = '1px solid rgba(255, 255, 255, 0.25)'; el.innerHTML = ` <span style="display:inline-block;width:8px;height:8px;border-radius:50%;background:#ffffff;box-shadow:0 0 8px #ffffff;animation:edgePulse 1.5s infinite;"></span> <span>${text || 'Çevrimdışı Mod'}</span> ${count > 0 ? `<span style="background:rgba(0,0,0,0.3);padding:2px 6px;border-radius:6px;font-size:11px;font-family:monospace;">${count} kuyrukta</span>` : ''} `; el.style.transform = 'translateY(0)'; el.style.opacity = '1'; } else if (state === 'syncing') { el.style.background = 'rgba(99, 102, 241, 0.92)'; el.style.color = '#ffffff'; el.style.border = '1px solid rgba(255, 255, 255, 0.25)'; el.innerHTML = ` <span style="display:inline-block;animation:edgeSpin 1s linear infinite;">⚡</span> <span>${text || 'Senkronize Ediliyor...'}</span> `; el.style.transform = 'translateY(0)'; el.style.opacity = '1'; } else if (state === 'online') { el.style.background = 'rgba(16, 185, 129, 0.92)'; el.style.color = '#ffffff'; el.style.border = '1px solid rgba(255, 255, 255, 0.25)'; el.innerHTML = ` <span>✓</span> <span>${text || 'Tüm işlemler senkronize edildi!'}</span> `; el.style.transform = 'translateY(0)'; el.style.opacity = '1'; setTimeout(() => { el.style.transform = 'translateY(140%)'; el.style.opacity = '0'; }, 3500); } } const EdgeOffline = { version: '2.0.0', isOnline: () => typeof navigator !== 'undefined' ? navigator.onLine !== false : true, getNetworkInfo, on: (event, callback) => { if (!listeners.has(event)) listeners.set(event, []); listeners.get(event).push(callback); return () => EdgeOffline.off(event, callback); }, off: (event, callback) => { if (listeners.has(event)) { listeners.set(event, listeners.get(event).filter(fn => fn !== callback)); } }, registerHandler: (action, handlerFn) => { handlers.set(action, handlerFn); }, enqueue: async (action, payload = {}, options = {}) => { const item = { action, payload, priority: options.priority || 'NORMAL', maxRetries: options.maxRetries || 5, retryCount: 0, timestamp: Date.now(), status: 'pending' }; const db = await getDB(); let recordId = null; if (db) { recordId = await new Promise((resolve, reject) => { const tx = db.transaction(STORE_NAME, 'readwrite'); const store = tx.objectStore(STORE_NAME); const req = store.add(item); req.onsuccess = () => resolve(req.result); req.onerror = () => reject(req.error); }); } else { const queue = JSON.parse(localStorage.getItem('edge_offline_queue') || '[]'); item.id = Date.now() + Math.random(); queue.push(item); localStorage.setItem('edge_offline_queue', JSON.stringify(queue)); recordId = item.id; } emit('enqueue', { id: recordId, action, payload }); const count = await EdgeOffline.getQueueCount(); if (!EdgeOffline.isOnline()) { updateBadge('offline', 'Çevrimdışı: İşlem Kaydedildi', count); } else { EdgeOffline.syncAll(); } return recordId; }, getQueue: async () => { const db = await getDB(); if (db) { return new Promise((resolve) => { const tx = db.transaction(STORE_NAME, 'readonly'); const store = tx.objectStore(STORE_NAME); const req = store.getAll(); req.onsuccess = () => resolve(req.result || []); req.onerror = () => resolve([]); }); } else { return JSON.parse(localStorage.getItem('edge_offline_queue') || '[]'); } }, getQueueCount: async () => { const queue = await EdgeOffline.getQueue(); return queue.length; }, remove: async (id) => { const db = await getDB(); if (db) { return new Promise((resolve) => { const tx = db.transaction(STORE_NAME, 'readwrite'); const store = tx.objectStore(STORE_NAME); store.delete(id); tx.oncomplete = () => resolve(true); }); } else { let q = JSON.parse(localStorage.getItem('edge_offline_queue') || '[]'); q = q.filter(i => i.id !== id); localStorage.setItem('edge_offline_queue', JSON.stringify(q)); return true; } }, clearQueue: async () => { const db = await getDB(); if (db) { return new Promise((resolve) => { const tx = db.transaction(STORE_NAME, 'readwrite'); tx.objectStore(STORE_NAME).clear(); tx.oncomplete = () => resolve(true); }); } else { localStorage.removeItem('edge_offline_queue'); return true; } }, syncAll: async () => { if (isSyncing || !EdgeOffline.isOnline()) return; const items = await EdgeOffline.getQueue(); if (items.length === 0) return; isSyncing = true; emit('syncStart', { count: items.length }); updateBadge('syncing', `${items.length} bekleyen işlem senkronize ediliyor...`); const priorityWeights = { HIGH: 3, NORMAL: 2, LOW: 1 }; items.sort((a, b) => { const pA = priorityWeights[a.priority] || 2; const pB = priorityWeights[b.priority] || 2; if (pA !== pB) return pB - pA; return a.timestamp - b.timestamp; }); let successCount = 0; for (const item of items) { const handler = handlers.get(item.action); if (handler) { try { await handler(item.payload); await EdgeOffline.remove(item.id); successCount++; emit('synced', { item }); } catch (err) { item.retryCount = (item.retryCount || 0) + 1; console.error(`[EdgeOffline] Error syncing ${item.action} (attempt ${item.retryCount}):`, err); if (item.retryCount >= (item.maxRetries || 5)) { console.warn(`[EdgeOffline] Max retries reached for ${item.action}, removing from queue.`); await EdgeOffline.remove(item.id); } emit('error', { item, error: err }); } } else { console.warn(`[EdgeOffline] No handler registered for action: "${item.action}". Skipping.`); } } isSyncing = false; emit('syncEnd', { successCount, remaining: (await EdgeOffline.getQueueCount()) }); if (successCount > 0) { updateBadge('online', `${successCount} işlem başarıyla senkronize edildi! ✓`); } }, bindForm: (formEl, actionName, options = {}) => { if (!formEl) return; formEl.addEventListener('submit', async (e) => { if (!EdgeOffline.isOnline()) { e.preventDefault(); const formData = new FormData(formEl); const data = Object.fromEntries(formData.entries()); await EdgeOffline.enqueue(actionName, data, options); if (options.onOfflineSubmit) options.onOfflineSubmit(data); formEl.reset(); } }); }, fetch: async (url, options = {}, fallbackAction = null) => { if (EdgeOffline.isOnline()) { try { return await fetch(url, options); } catch (err) { if (fallbackAction) { await EdgeOffline.enqueue(fallbackAction, { url, options }); return new Response(JSON.stringify({ offline: true, queued: true }), { status: 202, headers: { 'Content-Type': 'application/json' } }); } throw err; } } else if (fallbackAction) { await EdgeOffline.enqueue(fallbackAction, { url, options }); return new Response(JSON.stringify({ offline: true, queued: true }), { status: 202, headers: { 'Content-Type': 'application/json' } }); } else { throw new Error('[EdgeOffline] Network is offline and no fallbackAction provided.'); } } }; if (typeof window !== 'undefined') { window.addEventListener('online', () => { emit('online', getNetworkInfo()); setTimeout(() => EdgeOffline.syncAll(), 600); }); window.addEventListener('offline', async () => { const count = await EdgeOffline.getQueueCount(); emit('offline', getNetworkInfo()); updateBadge('offline', 'İnternet Kesildi • Çevrimdışı Mod', count); }); window.addEventListener('load', async () => { if (!EdgeOffline.isOnline()) { const count = await EdgeOffline.getQueueCount(); updateBadge('offline', 'Çevrimdışı Moddasınız', count); } else { EdgeOffline.syncAll(); } }); if ('serviceWorker' in navigator) { navigator.serviceWorker.addEventListener('message', (event) => { if (event.data && event.data.type === 'TRIGGER_OFFLINE_SYNC') { EdgeOffline.syncAll(); } }); } } return EdgeOffline; }));